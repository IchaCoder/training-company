import Nav from "@/components/nav";
import ViewDelegates from "@/components/view-delegate";

type Props = {};

const ViewDeletegatePage = (props: Props) => {
	return (
		<>
			<Nav />
			<ViewDelegates />
		</>
	);
};

export default ViewDeletegatePage;
